<%= form_with(model: record) do |form| %>
  <% if record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(record.errors.count, "error") %> prohibited this record from being saved:</h2>

      <ul>
        <% record.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :age_group %>
    <%= form.number_field :age_group %>
  </div>

  <div class="field">
    <%= form.label :stroke %>
    <%= form.text_field :stroke %>
  </div>

  <div class="field">
    <%= form.label :distance %>
    <%= form.number_field :distance %>
  </div>

  <div class="field">
    <%= form.label :swimmer %>
    <%= form.text_field :swimmer %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
